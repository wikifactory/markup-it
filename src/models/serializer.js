

class Serializer {

    matchType(types) {
        return this.then((state, node) => {
            
        })
    }

    /**
     * Create the serializing function from the stack
     * @param  {Function} fn
     * @return {Function} serialize
     */
    then(fn) {

    }

}

module.exports = () => new Serializer()
